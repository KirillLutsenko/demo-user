export const weatherApi = 'https://lab.lectrum.io/rtx/api/';

// export const getPersonages = () => fetch(`${weatherApi}/forecast`)
//   .then((result) => {
//     if (!result.ok) {
//       throw new Error(`${result.status}`);
//     }

//     return result.json();
//   });
